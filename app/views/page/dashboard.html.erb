<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <style>
    body {
      background-color: pink;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Dashboard</a>
    <a class="navbar-brand" href="#">Destinos</a>
      <a class="navbar-brand" href="#">Experiẽncias de viagem</a>
      <a class="navbar-brand" href="#">Meus Hosts</a>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <%= link_to 'Logout', logout_path, data: { turbo_method: :delete}, class: 'btn btn-outline-primary' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Deletar Contar', user_path(current_user), method: :delete, data: { confirm: 'Quer mesmo deletar a sua conta?'}, class: 'btn btn-outline-danger mx-2' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Editar Contar', edit_user_path(current_user), class: 'btn btn-outline-primary mx-2' %>
        </li>
      </ul>
    </div>
  </nav>

  <div class="container">
    <h1>Bem-vindo ao seu Dashboard!</h1>
    <p>Você está logado como: <%= @user.email %></p>
    <p>Seu nome de usuario é: </p>
    <%= yield %>
     </div>
</body>
</html>